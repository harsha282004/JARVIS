[project]
name = "jarvis"
version = "0.0.0"
description = "JARVIS - persistent, voice-controlled, AI-powered personal digital assistant (Phase 0 foundation)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
