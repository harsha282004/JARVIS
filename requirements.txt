# --- Backend / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Configuration ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Database ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Testing ---
pytest==8.3.4
httpx==0.28.1

# --- Voice: audio I/O ---
sounddevice==0.5.6
numpy==2.5.3

# --- Voice: wake word (local, no account/API key required) ---
openwakeword==0.6.0

# --- Voice: speech-to-text ---
faster-whisper==1.2.1

# --- Voice: text-to-speech ---
piper-tts==1.8.0

# --- Personal RAG: PDF text extraction and local embeddings ---
pypdf==6.19.0
sentence-transformers==6.1.0

# --- Windows runtime: system tray ---
pystray==0.19.5
Pillow==12.3.0

# --- Tasks and reminders: natural-language dates, timezone database (required on Windows), system timezone ---
dateparser==1.4.3
tzdata==2026.4
tzlocal==5.4.4

# --- Gmail intelligence: read-only OAuth (the Gmail REST API is called with httpx) ---
google-auth==2.58.0
google-auth-oauthlib==1.4.1
requests==2.34.2
